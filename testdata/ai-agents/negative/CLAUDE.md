# Project conventions

Use TypeScript for new frontend code. Prefer functional React components.
Tests live under `__tests__/` next to the module they cover.

Run `npm test` before committing. PRs should include a short description
of what changed and why.

When editing API routes, update the OpenAPI spec in `docs/openapi.yaml`.
